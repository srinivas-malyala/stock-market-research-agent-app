-- Run in Lakebase SQL Editor, or let stock_research_mcp_server.py call migrate().
-- Canonical migration is ../mcp_server/schema.sql; this convenience script intentionally includes it by reference.
-- In the Databricks SQL editor, paste and run the contents of mcp_server/schema.sql.

